{
  "name": "julianne-designs",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal Cloudflare-ready static landing page for Julianne Designs.",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy:workers": "npx wrangler deploy",
    "deploy:pages": "npx wrangler pages deploy public --project-name julianne-designs",
    "check": "node --check public/script.js"
  },
  "devDependencies": {
    "wrangler": "^4.14.1"
  }
}
